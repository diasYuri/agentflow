{
  "version": "v0.1.0",
  "changes": [
    "Add YAML workflow parser and validator",
    "Add DAG planner and Mermaid graph command",
    "Add local run persistence with JSONL events",
    "Add Codex provider behind AgentProvider port",
    "Add fan-out/fan-in execution with concurrency limits"
  ]
}

