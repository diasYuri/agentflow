version: "1"
name: extension-echo
description: Run a TypeScript extension through the AgentFlow Bun RPC adapter.

inputs:
  message:
    type: string
    required: true

nodes:
  - id: echo
    kind: extension
    extension: echo
    runtime: bun
    script: src/main.ts
    with:
      message: "${inputs.message}"
