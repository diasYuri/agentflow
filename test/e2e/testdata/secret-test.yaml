version: "1"
name: secret-test
secrets:
  api_token:
    env: AGENTFLOW_E2E_SECRET
nodes:
  - id: leak
    kind: bash
    command: echo "${secrets.api_token}"
    capture:
      stdout: true
      stderr: true
      exit_code: true
